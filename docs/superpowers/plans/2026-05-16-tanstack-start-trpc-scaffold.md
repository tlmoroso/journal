# TanStack Start Scaffold Archive Note

This document previously captured an early scaffold direction that is no longer used.

The project now uses TanStack Start server functions and TanStack Query without the older RPC stack.
